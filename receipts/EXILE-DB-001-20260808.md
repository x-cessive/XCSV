# EXILE-DB-001 Receipt - 2026-08-08

Work item: EXILE-DB-001 / GitHub issue #10

## Active File

ACTIVE_PROVEN: `E:\arma3server\@ExileServer\sql_custom\exile.ini`

Evidence:

- `E:\arma3server\Start_Exile_Server.cmd` starts from `E:\arma3server` with
  `-servermod=@ExileServer`.
- Current RPT loaded `E:\arma3server\@ExileServer\extDB3_x64.dll`.
- extDB3 logs are written under `E:\arma3server\@ExileServer\logs`.
- XCSV runbooks and `E:\ExileRepo\tools\extdb3\build-extdb3-stage.ps1` identify
  `@ExileServer\sql_custom\exile.ini` as the live extDB3 SQL_CUSTOM file.

The compatibility mirror
`E:\arma3server\@ExileServer\extDB\sql_custom\exile.ini` was byte-identical and
was synchronized for loader compatibility. Root-level `exile.ini` copies were
not edited.

## Hashes

- Before SHA256:
  `34101E4446598C4BE8772C44C877A42E95EC050B16F526D42AE199A98FCEA3FE`
- After SHA256:
  `10A5B55E96151C95354C50BEBFC543B5BC562FAF792937DE2CC2C7CCED2FFF66`
- Backups:
  `E:\arma3server\@ExileServer\sql_custom\exile.ini.20260808-141143.EXILE-DB-001.bak`
  and
  `E:\arma3server\@ExileServer\extDB\sql_custom\exile.ini.20260808-141143.EXILE-DB-001.bak`

## Repair

Four `$CUSTOM_1$` templates were repaired:

- `insertConstruction`: `territory_id = NULLIF(?, 'NULL')`,
  `SQL1_INPUTS = 1..12`.
- `insertContainer`: `territory_id = NULLIF(?, 'NULL')`,
  `SQL1_INPUTS = 1..18`.
- `updateContainer`: `territory_id = NULLIF(?, 'NULL')`,
  `SQL1_INPUTS = 1..14,17,15,16`.
- `createTerritory`: `flag_stolen_by_uid = NULLIF(?, 'NULL')`,
  `SQL1_INPUTS = 1..9,12,10,11`.

## Binding Evidence

Isolated extDB3 probe root: `D:\CAGE\xcsv-extdb3-probe`.

Direct extDB3 SQL_CUSTOM binding showed:

- `?` with literal `NULL` into nullable integer stored `0`.
- `?` with literal `NULL` into nullable varchar stored the string `NULL`.
- `NULLIF(?, 'NULL')` stored SQL NULL for uppercase and mixed-case `NULL`.
- Non-sequential and duplicated `SQL1_INPUTS` mappings are accepted by extDB3.

## Runtime Evidence

Restarted the test server after repair. New RPT:
`E:\arma3server\profiles\arma3server_x64_2026-08-08_14-13-47.rpt`.

Observed:

- `CallExtension loaded: extDB3`
- `ExileServer - Connected to database!`
- `ExileServer - Database protocol initialized!`
- `ExileServer - Game world initialized! Let the fun begin!`
- `ExileServer - Server is up and running! Version: 1.0.42`

No `$CUSTOM`, input-count, SQL_CUSTOM config, or extDB3 exception errors were
found in the new RPT or new extDB3 log.

Disposable persistence probe:

- `insertConstruction` returned `[1,[3,[]]]`; DB row had `territory_id =
  SQL_NULL`.
- `insertContainer` returned `[1,[1,[]]]`; DB row had `territory_id = SQL_NULL`.
- `updateContainer` returned `[1,[]]`; DB row had `territory_id = SQL_NULL` and
  `money=42`.
- `createTerritory` returned `[1,[1,[]]]`; DB row had `flag_stolen_by_uid =
  SQL_NULL`.

Cleanup verified zero `XCSVPROBE%` rows remained in `account`, `construction`,
`container`, and `territory`; scratch table `xcsv_extdb3_null_probe` was dropped.

## Continuity Status

Hermes profile `xcsvcontinuity` has no cloud API keys. The current continuity
role is fulfilled by durable XCSV baton files plus local tooling; no SOVRAN
credentials were copied.

OpenClaw profile `xcsvcontinuity` remains collision-sensitive because its
approval file copied shared authorization material from the default OpenClaw
profile. Do not use OpenClaw auto-routing for XCSV until this is resolved.
